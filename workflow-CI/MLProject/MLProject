name: Placement_Prediction_Project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: string, default: "preprocessing/processed_dataset.csv"}
      target_var: {type: string, default: "PlacementStatus"}
    command: "python modelling.py {data_path} {target_var}"